describe('Login con credenciales que no tienen privilegios', () => {
    it('Authentication error is raised when user doesnot have privileges', () => {
        //codigo que permite realizar la operacion
    });
});